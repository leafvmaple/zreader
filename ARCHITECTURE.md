# zreader 架构

这份文档写的是**为什么是这样**，不是每个函数做什么——后者看代码和注释。
读代码之前先读这个，能省掉大部分「这里为什么绕一圈」的疑问。

## 一句话

一个 TXT/EPUB/MOBI/PDF 都转成**同一种缓存 EPUB**、然后只按**字符偏移**对外
服务的阅读器；单个静态二进制，前端嵌在里面，SQLite 无 CGO。

---

## 1. 三条不变量

整个系统的形状由这三条决定，改任何一条都会波及很远。

### 1.1 缓存 EPUB 是唯一真相

源文件（TXT / EPUB / MOBI / PDF）**永不修改**。扫描时每个源都被格式化成
`<书库>/<作者>/<书名>.epub`，之后所有读取——正文、搜索、封面、导出、章节——
都只看这个缓存，不再回头看源文件。

带来的性质：

- **修了格式逻辑，重新扫描就能全量生效**，不需要数据迁移。这份文档里提到的
  几乎每个 bug 修复都是这样上线的。
- 缓存可以随时删除重建，它不是存储。
- 唯一的例外是**纯图片 PDF**：它没有可提取的文本，`book.Path` 直接指向源
  文件，由浏览器自带的 PDF 阅读器渲染（`cache_format = "pdf-image"`）。

### 1.2 字符偏移是通用坐标

阅读进度、书签、搜索命中、章节起点、`/content` 切片，全部用**扁平文本里的
rune 偏移**表示。不是字节，不是段落号，不是章节内偏移。

因此：

- 任何改变扁平文本的东西（章节识别、`<br>` 处理、清洗规则）都会移动所有已存
  的偏移。这就是为什么这类改动都要求重新扫描。
- 搜索的大小写/全角折叠必须**逐 rune 一对一**（`FoldForSearch`）。折叠后的
  字节偏移和原文不同，但 rune 偏移相同——扫描在折叠文本里进行，报出去的是
  rune 偏移，摘要从原文切。破坏这个 1:1 关系，所有命中都会指错地方。
- 纯图片 PDF 是这条规则的例外：它的 `char_offset` 和 `char_count` 里装的是
  **页码**。`atBookEnd` 因此要按格式分支——不分支的话，一千字符的「读完容差」
  会把一本一百页的 PDF 在打开的瞬间标记为已读完（真发生过）。

### 1.3 单个静态二进制

`go:embed all:dist` 把前端打进二进制，`modernc.org/sqlite` 避开 CGO，镜像
约 23 MB。这条约束否决掉的东西比它允许的更值得记住：

- **不做 PDF 光栅化**：纯 Go 的方案都很重，CGO 的方案毁掉静态二进制。
  所以 PDF 只有页级进度，没有页内位置——浏览器内置 PDF 阅读器不向外暴露滚动
  位置，`iframe` 同源也不行。
- **不做简繁转换**：唯一维护中的 Go 方案要在运行时从磁盘加载字典，二进制没有
  地方放。所以搜索折叠了大小写和全角，但繁体书用简体搜不到。
- **不用 CDN 字体**：阅读器原本默认从 Google Fonts 拉思源宋体，局域网无出口
  的机器上静默回退到系统字体，同时每次加载都向第三方发请求。现在三种内置字体
  全部解析到设备已有的字体，用户可以把字体文件丢进 `<data>/fonts`。
- OCR 和 `ebook-convert` 是**可选外部命令**，默认关闭，不在镜像里。

---

## 2. 后端分层

```
config   store   export   webui        ← 叶子，互不依赖
           ↑
        library                        ← 只有 scanner.go 用到 store
           ↑
        server                         ← 依赖以上全部
```

编译器保证无环。值得注意的是 `library → store` 这条边**只存在于
`scanner.go`**：其余 17 个文件是纯格式代码，不知道数据库存在。scanner 是把
「解析」和「持久化」缝起来的编排者，这条边是它的，不是整个包的。

| 包 | 职责 | 规模 |
|---|---|---|
| `config` | 环境变量 → Config | 40 行 |
| `store` | SQLite：书、章节、进度、书签、用户、会话、任务 | 1.8k |
| `library` | 格式解析、章节识别、缓存 EPUB 生成、扫描编排 | 8.3k |
| `export` | 清洗 + 按章切成训练语料 JSONL | 1.1k |
| `server` | HTTP、鉴权、中间件、SPA 兜底 | 4.7k |
| `webui` | `go:embed` 前端产物 | 33 行 |

`library` 是最大的包，因为格式的脏活都在这里：编码嗅探、章节模式注册表、
MOBI 容器、PDF 文本层、封面提取、OCR 兜底。

### 扫描的四个阶段

```
Phase 0  迁移    还原历史遗留的 <file>.txt.bak
Phase 1  格式化  每个源 → 缓存 EPUB（源文件只读）
Phase 2  入库    缓存 EPUB → 数据库行 + 章节表
Phase 3  清理    删掉缓存文件已不存在的行
```

扫描跑在后台 goroutine 上，进度写进 `library_jobs` 行，前端轮询。**不是**在
请求里同步跑——几百本书或一次 OCR 就是几分钟，任何代理超时都会掐断响应，而扫描
其实还在继续。

### 章节识别

`txt.go` 里是一张**规则注册表**（`chapterRules`），每条规则一个「标记形状」
（第X章 / 第X卷 / 楔子 / 「一」/ 一、）加一个层级 rank 和一个信任模式。
识别分两轮：先全量扫描每条规则，再按置信度打分取舍。

新增一种标记形状 = 在注册表里加一行，不需要动解析、合并、深度计算、EPUB 序列化
中的任何一处——它们都从这张表推导行为。

后面接一道 `StripTableOfContents`：转换来的 MOBI 常把自己的目录页也放在正文流
里，拍平之后那些行和真章节标题一模一样。判据是结构性的——**连续一串没有正文的
标题就是链接列表**——阈值从注册表的层级数推导（N 层最多产生 N 个合法的连续空
标题，N+1 个就不可能）。

---

## 3. 前端结构

```
pages/       ShelfPage (1.4k)   ReaderPage (1.4k)   AuthPage
components/  ReaderDrawer  ReaderTOC  ReaderSearch  ReaderSettings
             Dialog  AccountMenu  ExportDialog  BookCover  icons
hooks/       useWindowedList  useThrottledProgress  useModalFocus
reader/      settings.ts
api/         client.ts        types/  api.ts        auth/  AuthContext
```

两个页面仍然各约 1400 行，但现在各自只剩一件事：书架是「列表 + 筛选 + 批量
操作」，阅读器是「取一本书的文本，在屏幕上维持一个窗口」。再往下拆需要一个
返回二十个值的 hook，比一个长而内聚的文件更糟。

### 阅读器的窗口模型

阅读器**不整本加载**，也不是固定分页——单位是**章**：

- 冷启动只加载存档进度所在的那一章，然后后台预取相邻两章。
- 渲染窗口是一段连续的章节区间，滚到边缘前一屏时向下/向上扩展。
- 向上扩展要补偿 `scrollTop`，否则在上方插入内容会把读者往下推。
- 窗口**从上方驱逐**：读者位置上方超过三屏的章节会卸载，文本留在缓存里
  （最多 60 章）。不驱逐的话读三百章就是三百章挂在 DOM 里。

滚动处理器每帧只做一次（rAF 节流），章节顶部位置有缓存，失效键是
`scrollHeight + clientWidth`——每一种让表失效的方式（追加章节、字体换页、
排版设置改变）都会改变其中之一。

---

## 4. 两处缓存

| | 位置 | 上界 | 失效 |
|---|---|---|---|
| 扁平文本视图 | 服务端进程内 | 192 MiB，LRU | 源 mtime / size |
| 章节文本 | 浏览器内存 | 60 章 | 换书 |

服务端那份每本书存三样：原文、搜索用的折叠副本、`[]rune`。约 **10 字节/字符**
——17 本的语料全打开是 198 MB，最大的一本单独就 74 MB。并发的冷加载会共享同一次
解析（singleflight）：打开一本书会同时发三个请求，各自解析一遍的话，最大那本
要分配 1049 MB 而不是 350 MB。

---

## 5. 鉴权

Cookie 会话，HttpOnly + SameSite=Lax，token 在库里存 SHA-256。首次启动强制
建号（`setup_required`）。中间件是**白名单**：只有 health、auth/status、
auth/setup、auth/login、auth/logout 不需要会话，其余一律 401——新增路由默认
是被保护的，这个方向比黑名单安全。

登录做了节流（60 秒 5 次）和时间对齐（用户不存在时也跑一次 bcrypt），避免用
响应时间枚举用户名。

进度写入用**服务端时钟**做乐观锁：客户端回传上次读到的 `base_updated_at`，
服务端用自己的时钟盖新版本。早先是客户端发自己的墙上时钟，两台设备谁赢取决于
谁的表走得快。

---

## 6. 配置

| 变量 | 作用 |
|---|---|
| `ZREADER_PORT` | 监听端口 |
| `ZREADER_DATA_DIR` | `library.db` 和 `fonts/` 的位置 |
| `ZREADER_LIBRARY_PATH` | 一个或多个书库目录，按 OS 分隔符分隔 |
| `ZREADER_EBOOK_CONVERT` | Calibre `ebook-convert` 路径（MOBI 的兜底，原生读器优先） |
| `ZREADER_OCR` / `_CMD` / `_LANG` / `_TIMEOUT` | OCR，默认关闭 |

---

## 7. 测试策略

- **Go 单测**：格式解析全部用**合成夹具**——真实语料是私有的，二进制 blob 也
  没法评审。MOBI 容器、EPUB 包、PDF xref 都是测试里手工组装的，这也是唯一能
  刻意覆盖偏移表和边界的办法。
- **e2e**（`frontend/e2e/v08-smoke.mjs`）：真起一个服务端 + Playwright，覆盖
  首次建号、上传、扫描、阅读、搜索、书签、导出、键盘可达性。CI 的最后一道闸。
- **一次性探针**：性能和内存的结论都是在真实语料上量出来的，用完即删。提交信息
  里保留数字（例如「5850 次布局读取 → 0」「1049 MB → 350 MB」），因为**结论
  比脚本值钱**。

> `AGENTS.md`：书库内容是私有的。书名、作者、正文、以及从中得到的逐本数字指纹
> 都不能出现在被版本控制的文件里——包括提交信息、代码、测试和文档。
