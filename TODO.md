# Known Issues / Follow-ups

Running list of small things deferred during normal work. Add a line when
something is worth remembering but not worth fixing inline.

_(Currently empty — last two items, both around chapter-title quality in
`backend/internal/library/txt.go`, were resolved by adding the
`buildLogicalLines` wrap-rejoining pass and the parenthesised part-marker
alternative in `InlineChapterPattern`.)_
